# 扩展实施记录

可转移候选包已生成并验证：package-release.ps1核对双端内容、服务器校验和及dist/已测候选逐文件一致性；将双端、许可、设备说明、checksums打入独立时间戳ZIP，避免旧散列资源混入。48个内容文件从ZIP直接读回哈希一致，版本922aea50201094a1；路径和ZIP校验值见RELEASE_CANDIDATE_STATUS.md。未删除旧产物，也未将打包完整性宣称为物理设备验收。

公文包房间生命周期补齐：delivery-room.test.ts新增3项跨Room/MatchSession/Battle集成测试；8人携包重连保持角色和目标，普通输入继续推进后只交付一次；2/8人携包过期离场只归还一次，过期身份不能重连，分别验证弃权/比赛继续与房主交接。目标/可见性/雷达6文件23项回归通过。位置仅用于建立拾取/交付几何夹具，过期操作直接调用Room.expire；不是自然取包或网络30秒计时测试。无需规则升级或重打包。真实多设备验收已询问用户第二台电脑的可用性，其余未验收项继续保留。

联机战斗首屏修复：仅战斗进行中隐藏大厅名单，结算恢复以保留返回按钮。双浏览器生命周期验证断线重连后继续隐藏、结算恢复、返回大厅及再次开局通过。客户端生产构建/打包完成；8+16生产性能夹具复测通过并验证1920×1080视口画布完整可见，截图已检查。规则17不变；最新报告09:14:58Z，992/992夹具帧满载、451快照，RAF约60Hz、p95/p99均16.8ms。实际物理设备性能仍待验收。

生产浏览器性能采样已落地：npm run test:performance，规则17夹具8玩家+16AI，30秒1800个RAF间隔约60Hz，p95/p99均16.8ms、无>33.4ms样本、无页面/协议错误；997/997夹具帧满载。类型检查通过。报告及局限见BROWSER_PERFORMANCE.md；SwiftShader与1120×620画布不能证明真实硬件1080p性能。截图发现战斗大厅名单挤占画布首屏，下一步修正并回归大厅生命周期。

共享核心边界验收完成：新增tsconfig.simulation.json及check-simulation-boundary.mjs，覆盖全部共享模块、Battle及LocalSession传递运行时依赖。独立无DOM类型编译与neutral打包通过，40个文件无外部运行时依赖；明确审核Recoil为纯计算模块。npm run check已包含此检查，证据artifacts/qa/simulation-boundary.json。仅新增工具/编译配置/文档，不改变规则17运行时或现有候选包内容。

规则17最终本机回归与候选更新：npm run check通过，263项/53文件单测、证据校验、hygiene、类型检查及生产构建通过。双端已重新打包，内容922aea50201094a1；新增两端manifest内容标识，隔离服务器验证同时校验SHA256、双端内容与实际握手一致，以及合作配装/攻击确认/盾部署。当前生产离线3项通过。仍需浏览器性能、共享核心完整依赖边界及跨设备验收；不将这些包称为全部阶段完成。

2026-09-11最新验收调整：用户要求压测10分钟无明显异常即可，取消30分钟门槛。规则17原进程在780秒采样后按要求停止；23401/23401观测帧满载，8端确认差3tick，缓冲峰值54474字节，已输出样本未见明显异常。提前中断没有最终聚合JSON，不宣称本轮最终tick分位数、带宽或全部输入确认已复核；不为补齐报告重新压测。以下30分钟要求及运行中状态如有冲突，均为历史记录。

整体目标：按 MULTIPLAYER_EXPANSION_ROADMAP.md 完成阶段A–G及验收。当前已有联机与合作首版，整体仍未完成。以下历史记录保留当时的状态，不代表当前功能缺口；最新阶段出口见路线图。

## 8+16持续负载探针与后续集成

持续负载复核器补强：增加15秒采样覆盖/间隔检查，要求两队有效战斗计数、足够tick样本以及完整8路正数带宽，避免空数组every误判成功；明确数值门槛不等于内存趋势验收。旧规则11完整报告复核通过；带syntheticVerificationFixture标记的空带宽负例被非零退出拒绝。当前规则17压测同一进程仍运行，270秒8100/8100帧满载，确认差2tick；完整30分钟结果未得出。期间仅运行两次约1秒的离线JSON复核，未跑构建或浏览器测试。

规则17最终候选持续负载已启动：npx tsx tools/coop-load.ts 1800，8个真实WebSocket输入客户端+16AI合成满载。75秒样本2250/2250帧维持满载，当前确认延迟2tick，发送缓冲峰值54474字节；仅为运行中样本，未通过30分钟门槛或内存趋势复核。沿用同一进程，等待期间只做轻量文档/源码读取，未并行构建或重测试。脚本通过生命/弹药补充与坠落复位维持负载，不替代自然玩法、死亡周转或浏览器FPS。

ARCHITECTURE.md更新当前共享模拟/会话/协议/装备及发布边界，移除顶部“尚无联机”的过期结论，解释构造随机种子与内部Checkpoint的复现范围、公开视野裁剪、在线装备与本机养成分离。单人PvE自动通关继续为非阻塞诊断。

阶段B满员出生矩阵（规则17）：五地图全部12个兼容模式组合逐一建立8角色，普通模式4v4、合作8人同队；90tick无输入落稳后检查存活/地面/坠落界限、9个身体采样点非墙、两两出生空间不重叠，再集体死亡并经历真实复活/落稳重复检查，全部通过。合作矩阵延后波次启动以隔离出生几何，实际波次逻辑另有专项，未做单人通关要求。

MapObjectives新增cooperative要求，合作地图须有至少8个玩家出生点且空间不重叠，customMatch入口执行。出生矩阵/目标插槽/合作共31项专项及类型检查通过。身体采样不是所有像素几何证明，移动路线仍结合已有飞机240组平台导航验证。包仍为规则16候选，下一次打包需包含规则17校验。

阶段G组合回归与打包首批：完整35项开发浏览器测试通过，覆盖战役/账号/原规则实验场/地图/公文包/合作开局/刀盾/新增枪械/在线重连。单人PvE没有新增自动通关门槛。npm run package:game已更新离线客户端、9枪/刀盾/地图/联机说明；生产包3项断网/存档/静态服务验证通过。

新增npm run package:server，以esbuild将Node权威服务器、ws和共享地图/规则打为server.cjs，附START.cmd、ws许可、运行与局域网/TLS/房间恢复边界说明及SHA256清单。verify-server-package.mjs把单文件复制到系统临时目录、清空NODE_PATH启动，验证建房→合作配置→AK/盾配装→开局→输入确认→部署盾快照，无错误；报告artifacts/qa/server-package.json，内容版本71a4e24e5cb8828e。测试结束已关闭临时服务器。服务器包不含Node运行时，要求Node22.12+，未自动开放防火墙或公网服务。

客户端与服务器包均已生成，但最终版本30分钟负载、客户端FPS、完整出生/插槽边界及跨设备验收仍未完成，当前按候选包记录，不宣称全目标交付完成。

用户验收范围调整（2026-09-11）：单人PvE自动通关测试降级为可选、非阻塞诊断。停止为单人脚本胜局继续迭代驱动或难度，保留单人开局/波次/死亡复活/失败结算/记录基础检查；后续主线转回边界回归、多人性能与分发。历史单人失败记录保留，不计为发布阻塞，也不改写成已通关。

上一局后退版已终止：206.142秒第二波失败，报告08-34-00-349Z；没有仍在运行的单人复验进程。新巡查逻辑只有专项验证，按本次用户要求不再启动自动通关复验。

阶段D搜索诊断：后退版单人运行仍停留第一波，已确认原进程持续输出，未因观察超时重启。代码检查发现snapshot-only驱动在敌人全部不可见时只前往固定objective，不探索其他平台。新增SearchPatrol，仅接受公开导航点、自身位置和tick，维持目标直至到达或450tick超时再换点；发现敌人/死亡时重置。接入合作脚本的模式目标适配，未改真实游戏敌人坐标、规则或难度；后续进度输出新增可见角色位置/血量/弹药，便于定位停滞。

巡查专项与类型检查通过，验证目标保持、到点推进、超时绕过、重置/空图及不携带导航内部字段。新逻辑尚未参与已运行进程，不将其视为单人通关证据。

阶段D单人当前版本复验：规则16默认M4/USP在42.356秒第二波失败；合法Dragunov/沙鹰配装26.078秒第一波失败，均无网络错误，未调整伤害/波次/复活或写入胜局。network-match新增marksman配装方案和诊断计数（开火指令、可见敌人/存活/换弹/空弹匣快照、观察到的射击数、死亡位置及当时可见敌人），数据只来自客户端快照。

诊断版默认配装38.493秒在第二波失败，65次观测射击、5击杀。前两次死亡时可见敌人与玩家近乎重叠，第三次弹匣空；这提示枪口重叠/战术站位问题，尚不能证明是唯一原因。驱动增加距敌90px内且同层时检查后方地面/墙体后退的普通移动操作，保留空中跳跃路线；复验正在单独进行，未记为已通关。报告时间戳08-27-27-977Z、08-28-23-220Z、08-29-53-816Z保留失败证据。

阶段F自然混装整局：network-match增加第7参数mixed，按双方对称组合配置AK/刀、M4/盾、Dragunov/沙鹰、SAW/USP。驱动按接收快照重建可见装备与模式携带状态，特殊副手沿用BotController行为，枪械精确驱动按automatic属性触发。首局规则16/71a4e24e5cb8828e在68.141秒14:20结束，但盾部署观测为0，因此不算完整使用证据。修正测试驱动在等待快照确认时重复发送swap的切换问题后复验。

复验报告network-match-hijack-tdm-8-predicted-71a4e24e5cb8828e-2026-09-11T08-25-33-545Z.json：57.515秒/1727tick、13:20，8端终局一致，无服务器错误，仅终局在途拒绝；双方刀选中快照158/36次，盾选中77/21次，其中一方实际部署9次快照。约22.15–22.63KB/s/人。所有操作来自各端公开快照，未改权威坐标/血量/比分；快照次数不是攻击或拦截次数，不声称完成装备平衡或真人体验。后续mixed报告新增equipmentAccepted门槛，必须观察到已配刀与已部署盾，否则保留报告并失败。

最新npm run build、hygiene、archaeology:validate通过；分发包尚未更新。首局期间同机执行构建，复验期间同机执行来源/仓库校验，报告不作为独占环境性能基准。生产构建仍有Phaser大分块提示，未因此声称浏览器帧率达标。

阶段F新增枪械（规则16）：AK 47主枪与Desert Eagle副枪加入目录、职业购买/装备及在线统一开放列表，现有GunController/Arsenal处理射击和恢复，无Battle分支。首版AK伤害16/6tick间隔/30发，沙鹰34/15tick/7发，均为本游戏平衡值。原Guns_290的AK 47=25、Desert Eagle=7帧已目视确认，expanded-guns-art.ts原样复制PNG并附来源哈希；透明留白通过目录artFrame在Phaser纹理帧中处理，不改原图片。

ReferenceArt预加载枪械清单与枪长/帧区域从目录读取，移除原枪型长度条件。全套249项/51文件通过、类型检查通过。浏览器真实在线大厅选装→合作开局，验证AK持键多发、切沙鹰后持键仅一发/松开重按再一发，截图expanded-guns-deagle.png已目视检查握持与贴图。新增枪并非完整平衡验收，后续需最终组合回归和自然整局。

阶段F八客户端延迟伤害专项：network-offhand.test.ts通过真实服务器/8个WebSocket客户端、150ms RTT与10ms抖动代理，正常大厅配置刀/盾/副枪并准备开局。替换受控平地模拟以隔离伤害几何，随后只发正常输入：挥刀对举盾敌人单次40伤害且不耗盾，主枪正面命中消耗盾但不扣血，防御者通过瞄准输入转身后受到生命伤害且盾耐久不变；持刀者零枪械射击，接收快照反映盾状态，无网络错误/拒绝。专项及类型检查通过。

此项是8连接混合装备伤害链路验证，不是8真人自然整局、真实丢包、性能负载或公平性结论。近战与盾按权威当前tick裁决，未增加近战历史回溯；现有子弹历史目标回溯和当前盾朝向/耐久的组合仍需实际瞄准体验评估。

阶段F AI副手使用（规则15）：BotController通过普通swap/fire命令使用已配装刀盾。刀在近距离或主枪无弹且发现目标时选用，停止距离收紧到近战范围，攻击之间松开按键，沿用前后摇限制；盾在低血量时按时段部署并继续目标移动，破盾或防御时段结束切回主枪。携带公文包保持强制副手与模式导航，不因敌人在枪械射程内就停下举盾。普通枪械角色保留原决策路径。尚未给既有波次批量更换刀盾敌人，避免把行为适配误记为新敌人内容交付。

全套246项/50文件及类型检查通过。新增测试以真实Battle tick证明AI贴近/多次挥刀且不同时开枪、限时举盾/破盾退出、恢复后继续一致，并验证公文包携带导航。仍需8人混合装备与实际网络伤害/防御延迟验收、更多枪械内容、合作单人通关和最终版本性能/发布。

阶段F在线配装首版（规则14）：EquipmentLoadout仅接收合法主枪和副手ID，拒绝未知ID、错槽、额外伤害/养成字段；独立于本机职业等级与拥有状态。房间按连接身份修改自己的配装，变更取消准备，开局后拒绝；Battle仅允许frame0受信任初始化，并保存装备供复活/Checkpoint恢复。大厅可选现有枪械、刀和盾。公共角色快照增加OffhandView，在线角色/HUD复用单机表现，随既有可见性过滤整角色裁剪；内容指纹包含刀盾目录和规则数值。

全套243项/49文件通过、类型检查通过。双隔离浏览器完成双方分别选刀/盾、开局Q切换、普通攻击输入驱动挥刀/部署、零枪械射击、盾重连后保留选中状态；offhand-online-shield.png已目视检查。首跑发现开局按钮焦点令Q被过滤，修正场景就绪时释放大厅焦点并通过aria-busy标记加载状态，复测通过。仍待8人混合装备、近战网络命中/盾受击延迟、AI适配、更多新枪和最终回归性能/发布，不将两客户端动作测试当作完整网络验收。

阶段F单机操作闭环：军械库开放免费刀/盾副手卡片，切换到副枪可替换特殊副手；角色使用原始刀/盾正反面资源，保留导出透明画布，以实际控制器状态显示挥击、收盾/部署和耗尽灰色。HUD显示刀攻击状态或盾耐久/部署状态，特殊副手时不显示主枪弹药。OffhandView只向表现层提供种类/选择/动作年龄/方向/耐久/部署，不传命中去重集合。

两项真实浏览器测试通过：注册账号→军械库装备→自定义开战→Q切换→F攻击/举盾→确认未开枪和实际纹理可见→切回M4→返回军械库保持已装备。offhand-knife.png、offhand-shield.png均已目视检查，类型检查通过。在线装备状态/服务器配装选择、AI使用、完整混合装备回归和分发包仍待完成；本次只证明单机操作闭环，不替代网络验收。

阶段F配装数据与资源：Loadout.secondary正式扩展为枪械ID或knife/shield，SPECIAL_OFFHANDS登记行为/名称/说明；首版刀盾为免费一级基础副手。CareerProgress提供装备方法并兼容旧存档，未知副手回退USP，枪械仍检查拥有状态。Battle按配装创建控制器；特殊副手时Arsenal仅构造主枪，没有伪装成GunController的刀盾或额外USP。复活保持副手种类并重置状态。配装/职业/战斗34项专项和类型检查通过。

静态检查Guns_290时间轴确认Knife=63、Riot=56、Riotb=68（符号375），三张已有FFDec透明PNG已目视核对。offhand-art.ts原样复制至reference目录并记录源SWF、标签、帧号、尺寸及SHA256。未运行老游戏或生成替代素材。资源仍有原始透明留白，表现层需按握持位置/朝向适配；菜单、在线配装与公开装备状态尚未开放，尚不能把数据层可配装等同于玩家可操作闭环。

阶段F Battle行为接入：角色可持有OffhandController，内部Checkpoint显式保存/恢复控制器状态。普通切换尊重后摇，特殊副手选中时取消换弹并禁止枪械触发；每个存活角色tick推进刀盾，近战命中进入applyDamage。挥刀起手解除合作出生保护及隐身；盾防御读取控制器实时耐久/部署状态，复活创建同种新控制器，释放输入撤防。公文包强制副手并保存/恢复此前选择，保持携带期间禁止换枪。现有无特殊副手角色继续使用原Arsenal路径。

本批全套238项/47文件通过，TypeScript检查通过。新增真实Battle测试覆盖挥刀前摇恢复、实际单次扣血与枪械互斥、部署盾受击/复活、公文包强制副手和恢复。当前特殊副手仍由受信任模拟侧配置；合法Loadout、菜单、服务端配装校验、公开装备状态与动画尚未接通，不能声称玩家已能选用刀盾，也未更新生产包。

阶段F统一副手控制模块：OffhandController区分firearm/melee/shield，保留枪械弹道在Arsenal；刀按攻击按键边沿起手，持键切入不误挥，后摇期间拒绝普通切换，强制收起取消剩余命中但继续计时并禁止开枪。盾首版耐久120、连续持攻击6tick部署，松开/收起/死亡撤防，切换不补耐久，耗尽不能再次部署；数值属于本游戏设计。Checkpoint包含选择、持键、部署进度、耐久、挥刀时序与命中去重，并检查恢复状态。调用者必须每帧更新，包括收起期间。

副手/近战/盾12项专项测试及TypeScript检查通过，覆盖持续持键、强制收起后恢复、盾部署中恢复、切换耐久保存、破盾与非法恢复。当前仍是独立模块，尚未连接Battle角色生命周期、合法Loadout、网络和表现；不得据此宣称刀盾已可使用。下一步需显式序列化角色控制器并同步处理复活、配装、Arsenal枪械假设与公文包副手限制。

阶段F近战规则模块：MeleeSwing独立实现4tick前摇、3tick有效窗、12tick后摇，64px中心距离、120°扇区、40伤害；起手固定朝向，挥击原点随角色移动。像素间隔检查墙体，过滤友军/死亡/背后/超距，单次攻击以目标ID去重，输出DamageContext而不自行扣血。有效窗内后来进入的目标仍可命中，Checkpoint保存时序/方向/攻击序号/已命中集合。数值为本游戏首版设计，不宣称原版数值。

3项近战专项及类型检查通过，覆盖时序/重启限制、薄墙/过滤、有效窗恢复去重。尚未接入Battle输入、配装、动画和网络；下一步需统一副手装备行为，将刀盾与枪械明确区分，不能只给现有GunController挂零伤害武器。

阶段F盾防御核心：Shield.interceptShield实现本游戏首版120°正面子弹拦截，以实际攻击起点与防御者瞄准方向判断；消耗耐久，超出耐久的伤害穿透，耗尽自动收起。侧后方、爆炸、近战与环境绕过。Battle.applyDamage接入可选盾状态，出生保护/死亡/友伤不会误耗耐久，盾状态随内部Checkpoint恢复。当前仅规则核心，尚无合法配装入口、盾贴图、部署操作、复活重置或在线HUD，不宣称已可装备。

5项盾专项和3项伤害上下文测试通过，含真实Battle伤害/出生保护/恢复一致性，类型检查通过。角度和绕防规则属于本游戏设计，未声称原游戏数值。

阶段E边界补充：同tick双方最后一次交付更新两队分数后判平局；超时同分平局；载体落至killY以下立即复位并清携带武器状态。公文包Battle专项增至8项并通过，不冒称已覆盖全部真人/跨设备边界。

阶段F伤害入口第一批：DamageContext声明bullet/explosion/melee/environment、来源ID、起点、命中点与可选攻击ID。Battle.applyDamage作为共同入口，按当前权威角色解析来源，缺失来源/非法数值拒绝；旧damage保留为内部兼容适配。实际子弹传射线origin/end，手雷传爆心和目标中心，不再把射手位置当爆炸方向依据。现有职业修正/生命扣除/击杀/模式事件沿用，刀盾仍未接入，melee类型不等于已经可挥刀。

220项/42文件全套单元测试通过；之后新增3项上下文专项验证非法来源不改生命、友伤仍过滤、真实手雷tick传入爆心，均通过。类型检查在核心改造后通过。下一步装备行为/近战有效窗/盾方向与耐久、配装和存档迁移仍待实施。

单机雷达已接入：复用Radar投影与RevealPolicy，以本地权威Battle快照筛选当前可见角色，最多每2tick刷新；换Battle实例重置开火暴露历史和标记，暂停/菜单隐藏。雷达输入接口收窄为state/actorId/mode，不伪造网络消息。修正离线公文包旧提示“击败敌人得分”为运回基地、限制副手和目标分数。

地图浏览器两项通过：公文包开局可见双目标雷达，返回菜单隐藏，再开普通交火无残留目标；原飞机移动回归通过。类型检查通过，delivery-offline.png已目视检查雷达位置、队友/目标标记和模式提示。单机世界渲染仍保留原战役表现，仅雷达应用统一可见性；在线世界和雷达均由服务器过滤。

规则13自然公文包整局复验：9d332d358dee9d11，8个predicted客户端167.657秒、5031tick完成2:3，八端终局一致，无服务器错误，仅正常终局在途拒绝。每端只依赖已过滤快照，至少完成双方合计5次自然交付。报告network-match-hijack-ctf-8-predicted-9d332d358dee9d11-2026-09-11T07-37-48-782Z.json；压缩传输约21.82–22.11KB/s/人。该脚本仍非真人跨设备/浏览器帧率验收。

单人自定义菜单开放公文包：仅飞机可选，地图切换沿用模式兼容性回退。浏览器验证禁用属性、实际ctf开局/双目标存在、返回菜单未解锁下一战役任务；与原飞机移动测试共2项通过，类型检查通过，delivery-offline.png目视检查原蓝包/基地正常。首跑失败是Playwright通用toBeDisabled未识别已有disabled的option，改为直接验证disabled JS属性后通过。单机雷达尚未接入。

30分钟原进程已正常完成，无重启。原始报告07-05-20-136Z及review-coop-load.ts独立复核通过时长、满载、确认、实际战斗、tick预算、发送缓冲、错误与压缩带宽检查。54,002帧全程8+16，各端54,000输入全部确认；tick p95 0.523ms/p99 0.744ms，约56KB/s/人。预热后堆内存上下沿未见持续增长。详见[性能记录](PERFORMANCE_8P16.md)。只认证报告明示的规则11本机合成场景；后续“运行中”段落为历史进度，最终版本仍需复验。

在线战斗雷达：新增Radar纯投影/SVG表现模块，固定宽高视口保留地图比例、居中留边，复用原地图图像或矩形地形。白色自身、绿队友、红可见敌人、双方色菱形公文包与占点圆环；仅使用服务端已裁剪的当前StateMessage，快照刷新重建，无持久敌方位置缓存。固定在游戏右上角，不拦截鼠标，不改变模拟。单机雷达尚未接入。

投影专项覆盖五地图、两组视口尺寸和中心/边界/比例；双浏览器公文包测试新增雷达可见、两目标以及远端敌人标记缺失检查，通过。更新delivery-carrier.png已目视检查，原飞机底图、目标和携带表现位置正常。类型检查已通过；长期负载仍测启动时规则11，不包含新雷达浏览器开销。

公文包8客户端自然整局完成：规则12/c90bcbaf8743e354，158.284秒、4748tick、0:3，全部终局一致、无服务器错误，仅终局在途输入拒绝。由每端收到的目标快照驱动普通输入，未改权威坐标/生命/比分。报告network-match-hijack-ctf-8-predicted-c90bcbaf8743e354-2026-09-11T07-27-21-431Z.json。规则13过滤后的自然整局仍需复验，不把本报告当作过滤版本证据。

规则13服务端信息过滤：每场MatchSession独立RevealPolicy，接收者（含观战者）使用房间所属队伍视野，公开目标载体常显。VisibleState裁剪actors/poses、带隐藏目标的射线效果、视野外爆炸/手雷及非公开事件；ShotEffect新增来源actorId供裁剪，旧缺来源效果安全丢弃。客户端移动确认与自身运动基线保留。地图碰撞和目标基地是公开信息。此规则也影响世界角色绘制，雷达将直接复用接收后的数据。

规则13合作、公文包、PvP三项浏览器回归通过（21.5秒），包含观战、重连、交付和切局；雷达UI仍待实现。

全套216项初跑215通过，唯一失败为旧网络断言要求每端始终收到全部8角色。改为确认服务器仍8角色、远离敌人的客户端只见4队友、开火暴露跨重连仍有效。随后真实网络/可见性/多通道裁剪5项专项通过、类型检查通过；不是以删除容量验证规避失败。额外加入隐藏命中终点、朝向、爆炸及手雷不泄露的测试。长期负载原进程仍为规则11，最终报告只覆盖该版本。

雷达可见性规则准备：RevealPolicy按队伍共享存活队友620px内的无遮挡视野，队友与公开公文包载体常显，敌方cloak隐藏；受信任shot事件暴露60tick，重复事件不延长窗口，离场和过期记录清理。3项专项覆盖遮挡/距离/死亡观察者、开火窗口边界与去重、隐身/公开目标优先级，类型检查通过。尚未接入服务端快照过滤或雷达UI，因此当前在线仍广播完整角色，不能宣称信息隔离已完成。

network-match扩展ctf驱动：从每端公开deliveryTargets恢复该端的ModeRules目标状态，只通过普通输入移动/战斗；记录目标事件，若终局从未自然交付则判验收失败并保留报告。8端predicted运行已启动，已观测自然交付得分，仍待整局终局证据。30分钟合作负载原进程继续运行，不重启。

公文包在线首个闭环：失控飞机模式白名单开放ctf，目标3分，地图版本2；房间/协议类型及服务端配置校验贯通，在线大厅可选“公文包争夺”。ReferenceArt复用原蓝/橙包PNG，基地画对应色环，携带包跟随同一预测/插值坐标；HUD显示交付目标及双方目标状态。离线场景也具备绘制接口，单人自定义菜单尚未增加选项。

此批全套213项/39文件单元测试通过，未更新生产分发包。

新增双隔离浏览器专项：实际创建/加入/准备/选择ctf/开始，受控坐标触发拾取，Q不能切回主武器，断线重连后仍仅一载体，受控交付后两端比分1:0且恢复M4，交付事件仅一次。流程6.6秒通过，delivery-carrier.png已目视检查原包和基地圈正常；受控位置不算自然路线/八人整局验收。类型检查通过。此浏览器及后续核心回归与长期负载并行，最终性能报告需要注明这些同机测试活动。

公文包原美术确认：静态timeline查得MBFZ_fla.Guns_290符号375，flag1/flag2对应74/75帧，目视检查现有导出PNG确为蓝色和橙色公文包。新增delivery-art.ts原样复制两张112×68透明PNG并输出源SWF/符号/标签/帧号/尺寸/哈希清单；ReferenceArt预加载已注册，实际世界绘制待下一步接入。未启动原游戏、未生成替代图，也未重新解码/裁剪图像。

公文包离场与隐身边界：新增目标release操作及Battle统一目标事件处理，Room.expire在删除角色和可能触发弃权之前复位携带目标，避免终局停止tick后遗留悬空carrierId。重复释放不重复发事件。新增拾取清cloak、携带期间拒绝cloak、释放后可开启以及立即离场终局无需下一tick复位测试；6项公文包Battle测试和6项房间测试共12项通过，类型检查通过。ctf房间入口尚未开放，所以此处房间回归只证明既有流程未回退，仍需后续ctf真实网络离场验证。

公文包核心集成（规则12）：ModeRules增加ctf与可携带目标Checkpoint，Battle按tick接收目标事件，只有交付加团队分，击杀不加目标分；胜负复用中立结果。快照包含deliveryTargets，事件日志新增objective-pickup/delivery/return。AI目标选择增加携带回家、拦截己方目标载体、护送己方载体和取包。携带时保存原武器、强制副手并拒绝换枪，交付/复位恢复原选择；拾取解除cloak，携带时禁止再次开启cloak。

4项Battle专项加7项目标专项、2项原模式事件回归共13项通过，类型检查通过。Battle专项通过受控坐标测试单次交付胜利、死亡不加目标分、携带Checkpoint继续一致及AI目标选择；不属于自然寻路通关。当前未在地图模式白名单或房间菜单开放ctf，仍需网络模式类型/校验、可见性、目标世界表现、携带隐身专项、离场与重连综合测试。长期负载进程继续使用规则11版本，不因新增源码而热更新。

阶段B/E接口准备：MapGeometry新增可选deliveryBases双方目标插槽，飞机坐标直接来自plane-nodes.json的NodeCtfFlag提取结果（376.1,1008.85 / 2457.2,622.05）。MapObjectives提供结构兼容检查，区分战斗/据点/双方交付要求，报告缺失、非有限、越界与交付区域重叠；customMatch接入现有战斗/据点结构校验。尚未将ctf加入可选模式，其他地图不伪造基地。

3项专项验证所有现有地图结构、非法目标拒绝及两处飞机基地经过90tick真实碰撞落稳后仍能拾取；类型检查通过。插槽不是完整出生安全/导航连通性验收，公文包模式尚待集成。新增地图元数据会改变内容指纹；已启动的负载进程使用启动时版本8fe1dfc77c85059d，不热载新文件，也不会重启该验证。

负载等待期间先做阶段E静态规则及独立模块准备，未将D未过出口标成完成。BRIEFCASE_RULES.md整理双方交付+1/默认3分、死亡立即复位、己方目标离家可交付、携带副手限制的静态证据，修正此前假设的掉落回收规则。DeliveryObjectives只实现目标归属/拾取/交付/载体失效复位与Checkpoint，未接入Battle、模式选择、雷达或美术。7项专项测试及类型检查通过；本次仅小型专项与类型检查并行于负载运行，报告需保留同机工具活动这一限制，不宣称完全独占环境。

新增tools/coop-load.ts，运行命令`npx tsx tools/coop-load.ts 30`或`npx tsx tools/coop-load.ts 1800`。建立8个真实WebSocket命令客户端，受信任Checkpoint注入16个AI，保持普通模拟、寻路、射击、命中及网络快照；测试专用生命/弹药补充和坠落复位维持24角色满载。高生命和固定敌人数改变自然战斗条件，不能作通关、平衡、死亡复活负载或浏览器FPS证据，不向生产服务增加测试入口。

记录每tick耗时、饱和帧数、双方射击/伤害事件、30Hz输入确认、发送缓冲、事件循环延迟、应用及压缩带宽、进程CPU和15秒间隔内存样本。tick计时排除测试补充及记录开销；进程CPU/内存包括服务器、压缩和全部脚本客户端，非独立服务器口径。样本保存后仍需审查内存趋势，passed只代表脚本明确检查的项目。

30秒首跑报告artifacts/qa/coop-load-8fe1dfc77c85059d-2026-09-11T07-04-21-961Z.json通过：902个观测tick全部8+16存活，tick p95=0.647ms、p99=0.8951ms、max=4.0703ms；压缩WebSocket传输约68.4KB/s/人。此次首跑尚未包含后加的射击/伤害计数断言。类型检查通过；加入上述断言后的1800秒运行已启动，尚未宣称30分钟通过，待其报告与内存趋势审查。该运行期间不并发其他重型测试，以减少测量干扰。

## 最新验收：合作本机结果记录（2026-09-11）

### 人数分档同时在场上限（规则11，最新）

规则10八人复验在48.305秒自然通关，保留对应a7d7519fadf38455报告。随后扩展场景配置activeCaps（可选三档，缺省沿用activeCap），airborne-survival场景版本2配置为[2,8,16]，分别对应1–2/3–4/5–8人；总敌人预算、伤害、刷怪间隔与复活池不变。WaveDirector在每波边界选择并保存activeCap，当前波断线不降档，下一波重新按剩余人数选择，Checkpoint保留已锁定值。规则11指纹8fe1dfc77c85059d。

测试补充四人开波后减到一人仍保持8、恢复后保持8、下一波降为2及非法超限配置拒绝；原16敌人测试改用真实8份队员记录（旧fixture构造8人导演但只提供1个队员）。197项/36文件单元测试、类型检查、合作/PvP浏览器回归通过。

规则11自然对局报告位于artifacts/qa/network-match-hijack-coop-{人数}-predicted-8fe1dfc77c85059d-{时间}.json：1人07-00-37-961Z，38.717秒第二波失败，峰值2敌人；4人07-00-46-096Z，46.759秒通关30敌人，峰值5；8人07-00-48-966Z，49.598秒通关42敌人，峰值4。日期均2026-09-11，三局终局一致、无服务器错误，单人won验收正确非零退出。四/八人通关已有当前版本证据，单人通关仍未满足。

八人压缩传输约30.0–30.1KB/s/人，应用载荷约131.8KB/s/人。对局与回归并行运行，耗时数字不作独占硬件性能认证；自然峰值远低于16，下一步需独立8+16负载探针及长期测试，不把本次整局当作容量验收。

### 后续实战与出生保护规则（规则10）

规则9重复运行1/4人predicted自然对局，分别约35.2/49.0秒失败；补充伤害事件统计后再跑，分别30.9/52.2秒失败。四人后一次可解析敌方伤害1054.855，其中524.355来自接收快照时仍有出生保护的攻击者。统计按公开事件ID去重，保护状态取快照而非事件发生精确时刻，不能作为逐tick精确归因。

本游戏合作规则改为实际射出子弹即解除出生保护，玩家和AI相同，未开火时仍保留原75tick缓冲；空按键、等待和换弹不会主动解除。此项只影响合作模式，内容规则版本升至10、指纹a7d7519fadf38455。未降低敌人生命/伤害/数量，未运行老游戏。

规则10实测：1人21.753秒失败，仍缺自然通关；4人45.522秒自然获胜，30名敌人清除（玩家击杀29、环境死亡1），四端终局一致，无服务器错误，只出现正常终局在途拒绝。双方保护期间伤害快照统计均为0。四人压缩WebSocket传输约25.5KB/s/人，应用载荷约96.5KB/s/人；并行脚本运行不作为独占硬件性能认证。本局峰值8敌人，不满足8+16负载。

报告：artifacts/qa/network-match-hijack-coop-4-predicted-a7d7519fadf38455-2026-09-11T06-57-15-504Z.json；单人失败报告对应2026-09-11T06-56-51-235Z。工具新增第六参数any/won/lost；指定won时自然失败会非零退出并保留报告，不将“链路正常结束”误认为“通关通过”。旧规则8人通关不能替代规则10的8人复验。

新增合作开火/等待/换弹保护测试；196项/36文件单元测试、类型检查、合作/PvP两项浏览器回归通过。下一步仍需单人合作体验与正常通关、规则10八人复验、8+16负载和长期性能。

CoopRecords按房间实例UUID及局号去重，仅记录合作终局中绑定蓝队角色的结果；排除进行中、PvP和观战快照。本机浏览器保留最近50局，记录地图、场景、胜负、波次、击杀、阵亡和时长，不写战役养成或账号存储。房间实例ID避免服务器重启后复用房间码误判旧局。

修复存储配额不足时旧持久化列表覆盖内存新结果的问题，读取改为合并/去重；新结果写入失败仍保留本页内存，后续新局写入成功时一并恢复持久化。页面关闭前尚未持久化的结果仍会丢失，UI明确提示。此存储不是跨标签事务数据库或权威在线战绩。

新增5项单元测试覆盖终局参与资格、重复/刷新/房间实例去重、配额失败与恢复、50局上限、损坏/禁用存储和其他存储键隔离。浏览器用明确标记的服务器终局fixture验证显示一次、断线重连不重复、刷新后恢复及其他localStorage内容完全不变；该fixture不算自然战斗胜局。同时发现并修复终局文字覆盖断线提示的问题。

验证：195项/36文件单元测试、类型检查、合作及PvP两项浏览器回归通过。尚需1/4人自然通关、8+16与长期性能、跨设备体验及阶段E–G；未刷新分发包。

## 阶段A：进行中

已完成第一项核心改造：Battle新增tickPlayers，以actorId读取独立输入，跳跃边沿按角色保存；缺失真人输入回退中立输入，不继承本地主角输入。原tick接口保留为单人适配。换枪、换弹和道具接口可指定角色，释放输入覆盖全部角色。

验证：135项单元测试通过，包含新增的8名真人分别移动/开火/跳跃、缺失输入不串键、副手切换隔离；TypeScript检查通过。

第二批完成：新增MatchSession，按外部分配的玩家归属接收命令，校验有限坐标/布尔输入、限制队列、拒绝重复和旧序号；逐tick处理动作并回报实际消费序号。支持最多8个控制器绑定、输入超时清零、断线丢弃未执行命令。新增可恢复随机源和中立比赛结果读取接口（保留原单人phase兼容层）。139项单元测试及类型检查通过。

第三批完成：战役场景通过LocalSession采样键鼠，换枪/换弹/技能/道具走统一命令队列；采样按模拟tick执行，避免高显示帧率导致网络式队列积压。暂停清理尚未执行动作。30/60/144Hz一致性和暂停测试通过；141项单元测试、6项战役/养成浏览器测试通过。

第四批完成：Battle内部Checkpoint保存独立角色状态、移动锁定计时、生命、全部武器/共享冷却/后坐力、随机状态、输入边沿、模式积分、手雷来源关系与表现事件；MatchSession额外保存控制器归属、输入队列/确认序号和不足一帧的时间。恢复接口只用于受信任的内部状态，不作为网络入站协议。默认Battle随机源改为可保存状态，仍支持测试注入随机函数（无状态函数禁止生成Checkpoint）。

验证：JSON往返后，包含手雷/换弹/阵亡/AI的对局连续250帧完整内部状态一致；会话恢复保留待处理命令和去重状态。143项单元测试及类型检查通过。

第五批完成：TDM死亡计分、占点累计和中立终局规则移到ModeRules；Battle保留调用点与单人phase适配。模式状态包含于Checkpoint。新增有序且有容量上限的EventJournal，记录开火、伤害、死亡、终局；场景音效消费事件游标，恢复状态保留事件序号。占点部分进度恢复、争夺停止计分、红队获胜、终局只发一次及恢复后不重读已消费事件均有测试，145项单元测试通过。

第六批完成：BotController独立生成输入与动作，模式提供botGoal，机器人不再直接修改武器状态；Battle消费AI决策。8控制器录制、中途JSON恢复并继续108帧完整状态比较通过，146项单元测试、类型/构建及素材隔离检查通过。26项全套浏览器回归均通过。

阶段A剩余审查：共享规则与展示依赖边界、综合验收记录。当前8人测试为核心输入测试，不是联机或性能认证。

## 阶段B：静态资料准备

定位到飞机碰撞符号1323（MBFZ_fla.Wall_plane_231），现有首帧导出2874×1430。Arena.Init从wallMC绘制BitmapData，changeWallFrame能重绘；尚需核对碰撞颜色判定、坐标偏移、地图主体时间轴的出生/导航节点与是否实际换帧。不能将PNG中红色或黑色直接猜成实体。未运行原游戏。

已核对Movement.hitTest：只接受alpha=255像素。实际PNG同时含透明、完全不透明和多种半透明红色，因此不能按RGB判墙。Arena的plane标签在第5帧，wallMC放置(-3.55,-0.05)，但Arena.wall.draw使用符号本地空间，导出bounds与本地坐标仍须核对。第5帧包含1276出生点、1268动作点、1222双方目标点；部分记录仅更新位置，必须恢复完整显示列表继承/移除语义，不能只筛frame=5就作为最终节点清单。

新增CollisionWorld统一矩形与逐行区间位图碰撞，wallFor已接入；服务端不需要图片解码或DOM。147项单元测试、类型检查通过。飞机地图尚未可玩。

静态显示列表已补继承与移除解析。plane-map.ts提取16个导航点（按NodeWaypoint名称连接）、两队各8出生点和2个CTF节点。curate-plane.py将alpha=255像素编码为4545个逐行实体区间，保存hijack.json；地图图像1320与来源清单已加入精选资源。shape1322本地bounds从(0,0)开始，shape1321美术原点(143.45,312.1)。原坐标下16个队伍出生点使用现有移动核心推进90帧全部落稳，且头部不在墙内；专项测试通过。尚需实际地图选择、相机/美术对齐、导航动作与模式兼容验收。

已接入独立MAPS目录和customMatch构造器，菜单可选择现有地图或失控飞机、TDM或占点，进入1真人+7机器人的4v4对局，自定义结果不结算战役养成。飞机使用自己的世界高度和随玩家高度移动的相机、美术按静态原点定位。真实键鼠进入/移动/返回菜单专项浏览器测试通过，artifacts/qa/hijack-battle.png已人工检查。修正战役构造器显式Math.random导致不可Checkpoint的问题，现使用默认可恢复随机源。

剩余：地图定义与旧Mission完全解耦、缩略图/兼容性、导航动作与跨平台双向通行、出生安全及综合验收。生产离线包暂未更新到此批。

导航专项：tools/probe-navigation.ts逐条以实际OriginalMovement执行原图43条有向连接，原策略33条通过。新增Traversal边缘地板采样后38条通过，148项单元测试通过。剩余失败连接1→15、5→6、6→5、8→9、11→10（零基导航索引）；通过的38条仍构成16节点全可达图，但当前运行图未过滤失败边，最近点重选也可能影响长程行走，因此尚不能声称AI全图通行验收完成。报告位于artifacts/qa/hijack-navigation.json。

后续修正：trackedWaypoint保留当前路段直到抵达/目标改变/超时，避免空中反转；路径跟踪状态纳入actor.brain及Checkpoint，复活清空。运行导航跳过上述5条失败捷径，原始43条仍保留在hijack.json，几何未改变。tools/probe-routes.ts实际移动遍历16个平台之间全部240组有向路线，240/240通过，已加入hijack-map.test.ts回归；Checkpoint相关5项专项测试和类型检查通过。该结果验证无战斗干扰的导航，混合战斗/追击时的对局验收仍需进行。

完整对局验证：seed=12的8机器人TDM于2179帧以16:20结束，占点于2651帧以60:16结束；每名角色均有击杀与阵亡。发现坠落角色站在原碰撞边框交战，新增本游戏调校killY=1260（非提取原版死亡高度），落下后死亡并按151帧复活。复活优先空闲出生点，自定义旧地图补足第4个出生点。151项单元测试、类型检查通过，包含双模式正常达分和坠落复活专项。

地图定义已去除运行时剧情依赖：original-arenas.json保存四张原创地图几何，MapTypes统一几何类型，Mission引用MAPS。旧战役地形数值保留。缩略图、兼容矩阵和最终发布验收尚需收尾。

## 阶段C：房间核心开始

Room提供8名额上限、均衡分队、准备状态、房主配置、开局绑定MatchSession、离线输入拒绝及大厅房主离场继任。8人开局与角色控制隔离、大厅配置清准备测试通过；153项单元测试和类型检查通过。尚无WebSocket监听/在线客户端，重连令牌、结果返回大厅和网络验收待实现，不代表已经联机。

WebSocket服务已加入server/server.ts，npm run server默认监听127.0.0.1:4180；HOST/PORT可显式配置。服务端分配玩家ID、校验协议版本与消息、限制载荷/频率、按30Hz时钟推进房间并广播状态摘要。自动测试启动随机本机端口，以9条真实WebSocket连接验证8人开局、第9人拒绝及独立操作确认，通过；类型检查通过。当前广播仍为基础摘要，缺客户端渲染所需的完整表现数据和内容哈希。尚需在线界面/本地预测/插值、重连令牌、终局回房、延迟补偿和两设备验收。服务器未对公网部署。

新增StateMessage明确连接对应actorId、地图、朝向和表现事件；NetworkSession处理连接、输入序号与状态。/?online加入创建/加入/准备/开局界面，OnlineScene使用原资源绘制服务器状态并以30Hz提交键鼠命令，单人菜单有入口。类型检查和真实WebSocket专项通过，在线浏览器端尚未验收；目前直接渲染快照，无预测/插值，仍属开发中的基础连通版本。

双浏览器验收已通过：两个隔离Context经实际DOM创建/加入同一房间、准备、开局、加载Phaser画面；第二玩家键盘切换USP，第一玩家仍为M4，无页面异常。新增可访问DOM战斗状态用于检查，截图online-two-clients.png已人工查看。修复Node原生模块加载地图JSON所需import attribute。此测试在同一台机器，不替代两台物理设备/8人延迟压力验收；预测、插值和重连尚未实现。

远端位置插值已接OnlineScene：按服务器tick间距在前后位置之间绘制；丢失后续快照时停在最新位置，不无限外推；阵亡次数、存活状态变化或大位移直接对齐，避免复活横跨地图滑动。中间位置、停止外推和复活跳变专项测试/类型检查通过。自身移动预测、带抖动缓冲队列和重连仍待实现，当前不声称延迟手感验收通过。

自身Prediction已接入角色位置和相机，使用同一OriginalMovement与地图碰撞。服务器发送本角色完整移动计时及jumpHeld；确认后恢复权威状态并重放未确认命令。20步移动/跳跃中分批确认测试验证预测结果保持一致，最终完全收敛到服务器。只预测运动，不预测伤害/弹药/积分；专项测试、类型检查通过，真实延迟压力下修正量与丢包行为仍需验收。

重连第一批：服务器签发随机凭证，战斗断线保留30秒，恢复同一playerId/actorId和已接收输入序号；全部断线的房间在凭证到期后清理。真实WebSocket测试验证重连无重复角色、旧序号拒绝，类型检查通过。在线页新增手动重连按钮，清空预测和待发动作后接受新基线。尚需浏览器重连回归、过期席位/弃权处理、自动重试策略及界面状态完善。

重连第二批：凭证到期移除断线席位及其角色/投掷物，空队触发中立弃权终局（不伪造分数）；大厅房主可继任。Room专项测试通过。浏览器断网与传输中断回归发现点击重连后按钮保留焦点拦截战斗键，现由画布点击清理焦点。该专项复跑结果见下一批记录。

重连浏览器复跑已通过。新增房主地图/模式选择、终局返回大厅与round标识；新局重建Session、清准备和输入序号，客户端清预测/插值/画面，防止沿用旧局高帧号。4项Room测试通过（包括多局状态重置），类型检查通过。后续仍需完整在线多局浏览器验收及内容版本校验。

新增内容兼容指纹覆盖MAPS碰撞/导航/几何及装备/职业/技能/道具，规则语义版本显式维护；指纹仅用于兼容性，不作为防篡改签名。每条消息校验，真实连接测试确认错误版本创建房间被拒绝。双浏览器生命周期测试通过：用固定终局fixture结束首局，经DOM回大厅、换signal/占点、准备并启动第2局，画面重新加载；不把fixture当作在线实战通关证据。类型检查通过。

首次8连接负载探针：artifacts/qa/network-benchmark.json记录i7-14700KF、Node22.19.0、本机回环30.013秒，服务端推进901帧，无服务器错误。每客户端下行估算72KB/s；当前脚本按字符串长度计量，尚非严格UTF-8字节统计。每客户端仅发送645条命令（约21.5Hz），未达到满30Hz输入压力。646次advance调用p95为0.2352ms、p99为0.5742ms；这是定时器推进调用耗时，可能包含多个模拟tick，不是独立tick基准。无注入延迟/抖动，不替代30分钟、两台物理设备或8+16 PvE验收。

截至此批：最近一次单元测试记录为158项/28文件通过，类型检查通过；新增在线与地图浏览器专项通过，最新全套浏览器和离线发布包仍需重跑/更新。阶段A/B主要实现已有，C进行中，D–G尚未实施；各阶段均未宣称完成全部出口验收。

网络调度与带宽改进：服务端快照按单调时间15Hz调度，错过的快照合并，不再按定时器每两次回调发送。负载探针按累计时间补齐30Hz命令，计量UTF-8应用载荷（不含WebSocket/TCP开销），结束后等待全部命令确认并检查拒绝/错误。满频首测约100–101KB/s，发现最近32条事件每次重复广播；改为每连接/每局事件游标，仅成功排入发送队列才推进，重连重新建立近期基线。真实WebSocket测试验证事件在连续快照中只发送一次；接收方仍应按事件ID去重重连基线。

改进后2026-09-11T05:38:48Z报告：同一i7-14700KF/Node22.19.0本机回环，8连接30.010秒，每客户端900条命令，全部确认到899，无拒绝/错误；输入及模拟约29.990Hz，快照约14.962Hz，下行各约76KB/s，最大补发批次1。654次advance调用p95=0.2427ms、p99=0.4426ms；仍为整次推进调用而非独立tick。158项/28文件单元测试及类型检查通过。本轮未注入网络延迟、未验证两台设备，也未完成30分钟持续负载；这些仍是C/G出口条件。

跨局协议隔离：StateMessage及在线输入增加roomId/round，服务器拒绝错误房间/局次输入，客户端仅接受当前playing房间同局快照；内容规则指纹升至2，旧版本不能混连。重连清插值/预测/待发动作，保持服务器nextSequence。主动关闭或版本不兼容时保留具体提示，网络连接错误也不再被泛化断线消息覆盖。

双浏览器首跑暴露同帧终局丢失：endMatch/弃权可不增加模拟frame，客户端原先严格大于帧号才接收，终局及回大厅入口可能永久不显示。现允许有序WebSocket中同帧权威更新，仍拒绝更旧帧，并新增终局停止发输入测试。162项/29文件单元测试、类型检查通过；双浏览器创建/加入/准备/独立换枪/断网重连/fixture终局/返回大厅/换图换模式第2局复跑通过（7.8秒）。该fixture仍只验证生命周期，不代表8人实战胜负验收。C的历史命中补偿、延迟/抖动压力与跨设备整局仍待完成。

延迟验收基础：tools/network/delayed-proxy.ts在真实WebSocket两端按方向排队，注入50/100/150ms基础RTT、确定性±10ms单向抖动、每23条消息额外80ms阻塞。首版每消息定时器可能把同到期消息乱序，已改为单队列定时发送；不将代理错误当作游戏协议失败。此工具模拟应用消息延迟和有序队头阻塞，不声称模拟TCP真实丢包率。

network-delay.test.ts三档各8连接，以30Hz累计时间发送90条/人，720条/档全部得到单调确认，无错误或拒绝，各角色一次swap后均持USP。报告artifacts/qa/network-delay-{50,100,150}.json包含发送/确认延迟和代理积压。最新全套165项/30文件、类型检查通过。150ms档确认延迟p95约358ms、最大约437ms，包含阻塞、输入队列、模拟调度及快照等待，不等于基础RTT，也不证明手感达标。本批不含射击命中、客户端预测校正分布、真实TCP重传、跨设备或长时间测试；下一项仍为受服务端约束的历史命中补偿与对应延迟验证。

历史命中核心第一批：HitboxHistory保存最近5份tick起始命中盒，Battle.tickPlayers可接收受信任的actorId→历史帧映射，射击时仅替换目标命中盒，枪口/弹药/冷却/伤害仍走当前权威规则，静态墙检测不变。最多4tick回溯（133.3ms，低于150ms上限），非法/未来/过旧或无记录帧回退当前命中盒；历史死亡、出生保护、当前死亡/保护及deaths代次不匹配的角色不可历史命中。历史纳入内部Checkpoint，离线/AI默认仍使用当前命中盒。

验证：移动离开射线的目标在指定历史帧可被击中，当前帧射击则未命中；墙体挡住历史射击；复活代次和保护过滤、容量上限、保存恢复后真实射击完整状态一致。168项/31文件单元测试、类型检查通过。网络入口尚未生成或使用历史帧，不能称在线补偿已完成；下一批需要服务端测时/限定回溯预算、命令排队后过期处理，并在真实延迟代理下验证射击结果。当前不接受客户端自报历史帧。

历史命中网络接入：服务器每3秒发送随机probe，客户端仅回传nonce；LatencyBudget以服务器单调时钟测RTT，取最近最多8个且不超过10秒的样本最小值。估计单向RTT+名义15Hz插值间隔，接收时最多回溯3帧，为执行保留1帧，实际射击仍受4帧硬上限约束。没有有效样本时禁用回溯；错误nonce、重复回复、超过2.5秒回复无效。客户端可故意拖延回复，因此这只是有上限的估算，不是证明其真实网络延迟。

Room→MatchSession以独立参数传递authorityShotFrame；只复制公共命令字段，丢弃客户端附加shotFrame。历史帧随待处理命令保存/恢复，仅本tick新消费的命令携带回溯信息；超出窗口的排队命令回退当前命中，保持输入触发的后续开火不反复使用旧帧。内容规则指纹升至3。

验证：伪造/重放测时回复、过期样本、最小RTT及预算上限、命令字段伪造、排队过期、带元数据Checkpoint回放均通过。三档8连接代理测试验证服务器实际生成authorityShotFrame；150ms真实WebSocket射击专项通过，受控fixture在命令到达时移开目标，历史命中生效而伪造shotFrame=999999未被采用。fixture用于链路验证，不当作真人瞄准手感或真实移动预测验收。172项/33文件单元测试、类型检查、双浏览器多局/重连通过。仍需历史命中与客户端插值时刻对齐评估、预测修正分布、真实8人整局/跨设备及长期负载验收。

历史记录接入后8连接30秒回环复测（2026-09-11T05:54:46Z）：每人900命令全部确认，无错误；输入/模拟29.999Hz、快照15Hz、下行约77KB/s/人。657次advance调用p95=0.3573ms、p99=0.6867ms，硬件/测量口径同前。该结果包含历史记录和probe开销，仍不替代独立tick或30分钟性能验收。

预测校正测量：三档延迟代理测试运行真实Prediction，每次快照记录重放前后位移，输出p50/p95/max及最大5项的角色、帧号、确认序号、坐标、存活/死亡代次；全部输入确认后，8个预测移动Checkpoint分别与接收的权威移动Checkpoint完全一致。此测量为Node客户端运动层，不是浏览器帧率/视觉平滑测量。

发现并修正坠落过度预测：150ms档首次最大校正113.5px，定位为服务器越过killY后停止运动，而预测仍继续下落。Prediction现从地图读取同一killY，越界后只停止运动预测，不预测生命/结算；新增等待死亡快照时不继续下落测试。最新复跑150ms档406份校正样本p50=0、p95≈9.55px、最大≈48.34px，最大项出现在开局移动/跳跃尚未确认时；数值受定时器调度影响，不作为跨运行固定阈值。173项/33文件单元测试、类型检查通过。剩余大校正与远端插值时序仍需处理：当前插值按相邻帧间距运行，而历史预算使用名义66.7ms，抖动/快照合并下并非严格同一显示时刻；不宣称该时序已对齐或手感完成验收。

远端插值改为最多32份快照和32份到达偏移样本，按近期最低到达偏移估算服务器帧时间线，固定延后66.7ms绘制；展示帧不倒退，最多推进到最新已知快照，换局重置，死亡/复活和大位移仍不跨越平滑。不会每收到一包就从上一位置重新开始动画。Timing.ts统一服务器快照周期、插值延迟和回溯预算常量，内容规则指纹升至4。

验证：确定性抖动/连续到包、旧包、断流不外推、换局、复活测试通过；174项/33文件单元测试、类型检查和双浏览器重连/第2局流程通过（8.5秒）。这统一了插值延迟策略，但不是精确时钟同步：单向路径不对称、历史回溯整帧取整/上限、缓冲不足时停帧仍有误差，跨设备实际瞄准体验仍待验收。阶段C还需中途观战、8客户端完整玩法与长期运行；阶段D–G目标保持未完成。

中途观战：Room开局后允许在8个总名额内加入，新增席位标记spectator，不创建角色或绑定控制器；服务器拒绝其战斗输入，客户端不累计动作/发送输入，快照actorId=null、ack=-1。显示“观战（下局参战）”，画面跟随现有角色，Tab切换。观战者可断线重连，返回大厅后清观战标记，下局按正常准备/编队生成角色。弃权仅统计实际参战席位，不能靠观战者延续已空的队伍。内容规则指纹升至5。

验证：观战输入拒绝、无新增角色、重连仍无控制器、下局参与、观战席位不阻止空队弃权均有Room测试；176项/33文件单元测试、类型检查通过。浏览器生命周期扩为3个隔离Context：2人开局，第3人中途加入观战，尝试Q/Tab不增加角色，原玩家断线重连；fixture终局返回大厅后3人准备开第2局，服务器确有3角色且原观战页显示M4。流程通过（12.5秒）。仍未证明8人真实整局/跨设备验收；观战信息策略会随阶段E可见性过滤一起收紧，当前广播本身尚无隐秘状态隔离。

8连接自然整局工具：tools/network-match.ts从各客户端收到的快照更新独立决策模型，通过botInput生成普通网络命令；决策不读取或修改权威Battle，不调用endMatch，不更改生命/位置/比分。默认失控飞机TDM，也可传入地图及tdm/dom。脚本客户端不是8名真人或8个渲染浏览器，不能替代跨设备及视觉验收，驱动器也不作为双方技能/地图平衡评估。

整局结果：飞机TDM于63.610秒/1908帧达到11:20，8端终局及比分一致，每端1907条命令，无拒绝/错误；每端下行约84–85KB/s。报告network-match-hijack-tdm.json的shots字段是当前生命武器计数，并非全局累计（后续脚本已改名currentLifeShots）。占点首跑达到0:60，但终局通知前16条在途命令被拒绝，旧协议无原因，严格验收因此报错。服务器现明确返回match-ended或invalid-command，专项验证只在终局后使用前者，脚本仍拒绝所有比赛中的非法输入。占点复跑于64.268秒/1929帧达到0:60，每端1927条命令，无拒绝/错误，8端结果一致，下行约93KB/s。报告位于artifacts/qa/network-match-hijack-{tdm,dom}.json；这些短局数据不代替30分钟或4核参考机性能认证。

综合回归本批：npm run check通过（63条证据校验、317个可版本化文件资源隔离检查、176项单元测试、类型检查、Vite生产构建）；全套28项浏览器测试通过，包含离线战役/养成/训练场、飞机地图及在线三客户端流程。随后终局拒绝原因修改的真实WebSocket专项和类型检查通过。生产dist已刷新，分发离线包仍需在最终发布阶段更新。阶段C跨设备8人、浏览器帧率和长期负载仍待验收。

阶段B预览收尾：单人自定义菜单、联机大厅使用MapPreview显示当前地图原美术/地形、双方出生点和占点目标；模式选项遵循MAPS.modes，地图切换时回退到支持的模式，服务端仍由customMatch复核。类型检查、地图选择/实际移动及在线三客户端流程两项浏览器测试通过；hijack-menu-preview.png已目视检查。兼容性目前仍为模式白名单，不等于已实现PvE/CTF目标插槽验证。

tools/export-map-previews.ts为5张地图导出10份自包含SVG（预览与调试各1份），在artifacts/qa/maps下，原PNG内嵌，无外部文件依赖。调试图叠加飞机真实逐行碰撞掩码、运行导航有向边/编号、出生点、占点和killY；hijack-debug.svg已用Chromium渲染成hijack-debug.png并目视检查。红色覆盖为实体掩码，黄线为导航，红色虚线为本游戏坠落阈值；地图预览未运行老游戏或使用生成替代图。

## 阶段D：合作波次首版

新增coop模式和WaveDirector，Battle/Room/State/在线大厅已贯通；失控飞机可选择“合作生存 · 三波”，1人即可准备开局，最多8人同队，敌人单独生成且最多16名存活。其他地图暂不列为兼容。难度按波开始时的现存队员数分为1–2、3–4、5–8档，初始敌人数4/8/12，每后续波增加2，总三波；不放大生命。开局休整90tick、波间150tick、增援间隔24tick，出生点需离存活队员至少200px、离任何存活角色至少50px，无安全位置则等待重试。十分钟超时失败。

团队复活池为每名开局队员2次（最少2），死亡时按固定角色处理次序预留一次，151帧复活后清预留；耗尽后阵亡不能复活，全部阵亡且无预留则失败。敌人不自动复活；清空本波才进入下一波，三波清空获胜。离场不会修改当前波预算，不再触发PvP的“仅剩一队即胜利”逻辑；下波再按剩余队员调整。死亡敌人清理时保留未消失投掷物的来源对象。导演状态/预留/敌人序号纳入Checkpoint；规则指纹升至6。

新增9项PvE测试：1/4/8人的三波成功（分别18/30/42敌人）与复活耗尽失败、Checkpoint连续150帧恢复一致、离场不误判获胜/重置当前波、16敌人预算及复活仅预留一次。胜负测试使用权威伤害fixture清除角色，不冒充正常战斗通关。恢复测试暴露旧Navigation把OriginalMovement整体展开进route的隐含函数引用，现只保存x/y，修复追击路线无法序列化。185项/34文件单元测试、类型检查通过。

浏览器单人合作房间实际选模式/准备/开局，观察第一波服务器生成AI，键盘切USP和波次/复活HUD正常；原三客户端PvP/观战/重连/第2局回归通过。coop-online.png已目视检查。此为可运行首版，尚缺独立PvEScenario内容注册、无安全刷怪位置长期占用策略、1/4/8正常战斗整局、8+16性能及合作独立结果记录；C跨设备与长期负载也未完成，不能宣布D验收通过。

PvEScenario独立注册：PvEScenarios.ts定义airborne-survival v1及地图适配、时限、各波三档预算、存活上限、增援间隔、休整、复活和安全距离。配置有边界校验；customMatch解析并复制场景到Mission，WaveDirector保存当局配置，Checkpoint恢复使用保存的场景，避免默认注册表变化改变进行中的对局。HUD波数读取配置，兼容指纹覆盖场景且规则升至7。当前内容数值保持原三波首版设置。

新增场景配置/恢复、独立副本、人数档、存活上限、无复活配置、非法配置/不支持地图及配置指纹变化测试；187项/34文件单元测试、类型检查、浏览器合作开局复跑通过。仍待正常PvE战斗整局、刷怪阻塞退路、独立合作结果记录、8+16与长期负载；不将这些配置测试作为实战通关证据。

刷怪阻塞退路：CoopSpawn优先敌方入口，无安全点时尝试地图另一队已验证的出生点，保持玩家/角色安全半径，不生成任意新坐标。若所有点均被占用，WaveDirector记录spawnBlocked，HUD提示“增援入口受阻，请离开入口”，保留待刷预算并逐tick重试，原十分钟比赛时限仍生效。恢复安全位置后正常刷怪并清提示；该状态纳入Checkpoint，规则指纹升至8。

验证：敌方入口被占时选择备用点、全部占用时不生成、死亡单位不阻挡、阻塞不消耗预算及保存恢复后释放位置继续增援均通过。189项/34文件单元测试、类型检查、浏览器合作开局通过。仍需1/4/8正常战斗胜负、合作结果记录及8+16性能；当前等待策略会要求玩家离开入口，并未把守住全部入口算作过关。

PvE正常对局探针：network-match.ts扩展为1–8连接、动态快照角色模型及coop，支持basic/precise/predicted三个驱动策略。后两者使用快照瞄准可见敌人，predicted还使用真实客户端Prediction估算自身移动；只发常规命令，不修改权威状态/数值。新增死亡事件来源、波次和峰值存活敌人数，报告按内容版本保存。并行对局用于玩法链路验证，不作为独占硬件性能认证。

首批结果：basic的1/4/8连接均自然失败（约27/38/48秒），8连接达到第二波，所有终局一致，无服务器错误。precise推进更远；predicted的8连接在旧规则8的一次运行中58.149秒清除42敌人自然获胜（network-match-hijack-coop-8-predicted.json），并非fixture胜利。1连接死亡来源显示三次均为坠落，定位到BotController进入射程时会在空中停步，已改为仅落地后停步交火；新增专门测试，飞机路线/对局测试仍通过，规则升至9。

当前内容2fd5ca36cd77ba41复测：1/4/8连接分别约34/44/62秒自然失败，8连接清掉41/42敌人；死亡已主要来自正常战斗，终局一致，无服务器错误，只允许终局在途命令的match-ended拒绝。190项/35文件单元测试及类型检查通过。当前版本尚无完整1/4/8自然获胜矩阵，不能把旧版本8连接胜局当作全部通关验收。8连接PvE下行实测约136–150KB/s/人，超过初始100KB/s预算；本批峰值最多11个活跃敌人，也没有达到8+16负载。因此继续需要难度/正常通关验证、带宽优化、完整8+16与长期运行、独立合作记录。

网络传输压缩：WebSocket协商permessage-deflate，单消息超过1024字节压缩，级别3、最多4个压缩任务并行、双向禁用跨消息字典；不支持压缩的客户端仍可连接。测试覆盖同房间压缩/未压缩连接、重连，浏览器专项断言所有连接确实协商压缩。maxPayload与发送缓冲限制保持生效。

benchmark-network/network-match现同时记录解压后UTF-8消息和net.Socket.bytesRead差值：后者包括WebSocket帧，不包括TCP/IP头和TLS。保留旧kbPerSecond字段为应用载荷，新增wireKbPerSecond，不将两种口径混用；另记录本进程CPU用时（包含服务器、脚本客户端及压缩，不是服务器独立CPU）。对局报告后续加时间戳，避免同内容版本重跑覆盖证据。

2026-09-11T06:42:00Z当前规则9/内容2fd5ca36cd77ba41：8客户端predicted策略自然通关，61.914秒、1859帧、42敌人清除，全部终局一致，无错误/拒绝。应用载荷约136.6–136.8KB/s/人，压缩后WebSocket传输约31.6–31.7KB/s/人；advance调用p95=0.5314ms、p99=0.7529ms不含异步压缩耗时，本进程CPU合计约5.796秒。190项/35文件单元测试、类型检查、合作/PvP浏览器回归通过。当前版本8人自然获胜已有证据；1/4人获胜、8+16（本局峰值仅5敌人）、30分钟、浏览器帧率及独立合作结果记录仍未完成。未压缩回退客户端不享有这一带宽结果。
