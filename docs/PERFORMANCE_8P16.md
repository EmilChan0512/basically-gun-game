# 8玩家＋16敌人：30分钟合成负载记录

2026-09-11最新验收调整：用户要求压测10分钟无明显异常即可，取消30分钟门槛。规则17原进程在780秒采样后按要求停止；23401/23401观测帧满载，8端确认差3tick，缓冲峰值54474字节，已输出样本未见明显异常。提前中断没有最终聚合JSON，不宣称本轮最终tick分位数、带宽或全部输入确认已复核；不为补齐报告重新压测。以下30分钟要求及运行中状态如有冲突，均为历史记录。

测试日期2026-09-11，内容指纹8fe1dfc77c85059d（规则11）。Intel Core i7-14700KF，Node v22.19.0，本机WebSocket回环。不是4核参考服务器或跨设备认证。

命令：`npx tsx tools/coop-load.ts 1800`。原始报告：`artifacts/qa/coop-load-8fe1dfc77c85059d-2026-09-11T07-05-20-136Z.json`；复核命令：`npx tsx tools/review-coop-load.ts <原始报告路径>`，输出同名-review.json。

| 检查 | 结果 |
| --- | --- |
| 运行时长 | 1800.005秒 |
| 模拟频率 | 30.0005Hz |
| 满载帧数 | 54,002 / 54,002帧均为8名玩家＋16名存活AI |
| 输入 | 每端54,000条，全部确认至序号53,999，无网络错误 |
| tick | p50 0.270ms、p95 0.523ms、p99 0.744ms、最大6.094ms |
| 压缩下行 | 每端55.88–55.98KB/s，不含TCP/IP/TLS头 |
| 最大服务端发送缓冲 | 52,867字节，低于512,000字节截断阈值 |
| 最大输入确认差 | 4条命令，无持续积压 |
| 实际战斗计算 | 双方射击事件63,575 / 28,814，伤害事件67,390 |
| 整个进程CPU | user 122.672秒、system 42.343秒，含8客户端与压缩 |
| 事件循环 | p95 33.19ms、p99 34.14ms、最大43.29ms；与tick耗时为不同口径 |

内存审查排除前120秒。后续各5分钟区间堆内存下沿约13.84、13.56、13.09、12.26、12.44、11.52MiB，上沿约25–26.54MiB；未见堆占用包络持续抬升。RSS上沿从约124MiB到129MiB，有分配器和测量数组开销，不能仅凭RSS变化判泄漏；本次也不能证明任意时长无泄漏。

本次合成场景经受信任Checkpoint固定24角色，持续补生命/弹药、坠落复位；保留AI、移动、碰撞、射击、命中和真实网络消息。tick计时排除测试补充及记录工作；进程指标包含全部脚本客户端。测试期间同机还运行过专项、浏览器回归和一场公文包自然对局，未宣称独占机器。

结论：规则11在本机通过上述30分钟合成满载检查。没有验证死亡/复活周转负载、参考4核容量、多房间、真实网络、浏览器帧率，也不包含之后新增的规则13可见性过滤。最终阶段G仍需最新版本复验，不能因这份报告将整体目标标成完成。
