# 素材隔离

当前全部画面用原创几何图形绘制，无原版运行素材。`public/assets` 只允许可发布素材。原版 SWF、ActionScript、导出图像/声音/符号映射、参考图以及可能包含源码片段的索引位于 gitignored 目录。人工证据应只记录必要事实及来源，不复制大段代码。

以后按语义 ID 映射素材路径，再替换为原创或生成素材。当前不增加 ComfyUI 依赖。提交前检查 `git ls-files` 和 `git check-ignore`；禁止强制加入研究资产。
